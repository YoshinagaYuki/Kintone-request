/**
 * 申請フォーム表示内容のフォールバック既定値。
 *
 * 表示内容(FMTテンプレート/注意事項/案内文章)は form_types に保存され、
 * 管理画面「申請フォーム設定」から編集できる。
 * DBが未登録(空文字)の場合のみ、ここの既定値を表示する。
 *
 * ※ 改行・全角スペース(　)・インデントをそのまま保持すること(FMT解析に影響するため)
 */

/** FMTテンプレート(てずくーる 初期値) */
export const DEFAULT_FMT_TEMPLATE = `取次店名:
イベントブース名:
◯月分として請求:
配送料:
《コンテンツは必要箇所のみご入力ください》
コンテンツ1:
　数量1:
コンテンツ2:
　数量2:
コンテンツ3:
　数量3:
コンテンツ4:
　数量4:
コンテンツ5:
　数量5:
コンテンツ6:
　数量6:
コンテンツ7:
　数量7:
コンテンツ8:
　数量8:
コンテンツ9:
　数量9:
コンテンツ10:
　数量10:
《配送》　※「建物名」「店舗名」などまで記載お願いします。
　日付:
　郵便番号:
　住所:
　受領者氏名:
　連絡先:
《集荷》※「建物名」「店舗名」などまで記載お願いします。
　日付:
　郵便番号:
　住所:
　当日引渡者氏名:
　連絡先:
《伝票番号連絡先》　※任意のため必須ではありません
to:
cc:
緊急時責任者氏名:
緊急時責任者電話番号: `;

/** 案内文章(input_guide 初期値) */
export const DEFAULT_INPUT_GUIDE =
  "レンタルプランは上のプルダウンから選択してください。テンプレートをコピーし、各項目の「:」の後に値を入力して貼り付けてください。使用しないコンテンツ欄は空欄のままで構いません。";

/** 注意事項(notes 初期値) */
export const DEFAULT_NOTES = `・シールは「コンテンツ1」に記載してください
・コンテンツはkintoneの選択肢と同じ名称で入力してください
・◯月分として請求は「7月分」の形式で入力してください(自動で「7月分8月請求」に変換されます)
・日付は 2026/07/18 のような形式で入力してください`;

/** DB値が未登録(空)の場合のみ既定値へフォールバックする */
export function withFallback(value: string | null | undefined, fallback: string): string {
  return value && value.trim().length > 0 ? value : fallback;
}
